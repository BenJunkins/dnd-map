<div class="region-bestiary result-card">
  <header>
    <h3>{{region.name}}</h3>
    <p class="rb-meta">{{terrain}} &middot; {{region.danger}} danger</p>
  </header>

  {{#if empty}}
    <p class="rb-empty">{{localize "REGION_BESTIARY.Chat.Empty"}}</p>
  {{else}}
    <ol class="rb-list">
      {{#each entries}}
        <li class="rb-entry {{#if this.signature}}rb-signature{{/if}}">
          <div class="rb-entry-head">
            {{!-- data-rb-uuid is wired up client-side in scripts/chat.js, which
                  decides per viewer whether the link should work at all. --}}
            <span class="rb-name" data-rb-uuid="{{this.uuid}}">
              {{#if this.img}}<img src="{{this.img}}" alt="" width="24" height="24">{{/if}}
              <span>{{this.name}}</span>
            </span>
            {{#if ../reveal}}
              <span class="rb-stats">{{this.type}}{{#if this.cr}} &middot; CR {{this.cr}}{{/if}}</span>
            {{/if}}
          </div>
          {{#if ../reveal}}
            <p class="rb-reason">{{this.reason}}{{#if this.pack}} &middot; {{this.pack}}{{/if}}</p>
          {{/if}}
        </li>
      {{/each}}
    </ol>
  {{/if}}
</div>
